.. py:currentmodule:: larray_editor


Fixes
^^^^^

* fixed autocompletion in the editor console when using some particular dependencies versions
  (closes :editor_issue:`220`).

* fixed the "Update LArray" shortcut in Windows start menu when LArray is not installed in the "base" conda
  environment (closes :editor_issue:`211`).
